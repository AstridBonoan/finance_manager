{
  "name": "@finance-app/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for Finance Manager",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "eslint": "^8.50.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
