{
  "name": "@finance-app/db",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma database client and schema for Finance Manager",
  "main": "index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:prod": "prisma migrate deploy",
    "seed": "node --require ts-node/register prisma/seed.ts",
    "studio": "prisma studio",
    "format": "prisma format"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.3.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@prisma/client": "^5.3.0"
  }
}
